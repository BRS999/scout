# Scout Environment Configuration
# LM Studio connection (use localhost in dev; docker can reach host via host.docker.internal if needed)
LMSTUDIO_URL=http://localhost:1234/v1
LMSTUDIO_API_KEY=lm-studio

# Local model configuration  
LOCAL_MODEL=openai/gpt-oss-20b

# Database and external services (local defaults; docker overrides in compose)
CHROMADB_URL=http://localhost:8000
SEARXNG_BASE_URL=http://localhost:8080
SQLITE_PATH=/data/memory.db

# Application settings
NODE_ENV=production
HOST=0.0.0.0
OPENAI_API_KEY=lm-studio
PORT=7777

# Backend specific settings
SEARNX_URL=http://localhost:8080
NEXT_PUBLIC_BACKEND_URL=http://localhost:7777
