{
  "name": "@agentic-seek/agents",
  "version": "0.1.0",
  "description": "Specialized agent implementations for AgenticSeek",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format .",
    "format:write": "biome format --write ."
  },
  "dependencies": {
    "@agentic-seek/shared": "*",
    "@agentic-seek/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": ["dist"]
}
